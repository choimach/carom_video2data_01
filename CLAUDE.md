# 이 저장소에서 일하는 법

세션이 바뀌면 대화는 사라진다. 여기 적힌 것만 남는다. **고치기 전에 읽어라.**

## 0. 목적 (규칙)

> 프로 당구 선수들의 실전 데이터를 모아서, 내가 실전에서 마주친 공 배치에 대해
> 최선의 공략 방법을 trajectory와 각 공의 움직임을 통해 보여주는 것.

똑같은 배치는 데이터에 있을 수 없다. **가장 비슷한 배치를 찾아 추론한다** —
지식은 이웃에게서, 궤적은 이 배치에서. 자세한 것은 `PURPOSE.md`.

## 1. 토큰을 쓰는 순서

이 프로젝트에서 토큰을 가장 많이 먹은 것은 당구도 물리도 아니었다. **같은 것을
다시 알아내는 일**이었다 — 무엇이 돌고 있나, 어디까지 됐나, 저건 왜 저렇게
했더라. 세션이 압축되면 답이 사라지므로 또 알아냈다.

**① 세션을 열면 한 번:**

```bash
bash tools/state.sh        # 돌고 있는 것 · 단계별 개수 · 채점표 · 진행 중인 일
```

스무 줄이면 끝난다. 이걸 안 하면 같은 것을 알아내는 데 호출 예닐곱 번이 든다.

**② 데이터를 맥락에 쏟지 말 것.** `model.json`은 1,500판이고 `app-data.json`은
840 KB다. 읽지 말고 **물어라**:

```bash
~/.venvs/carom/bin/python -c "...집계만 출력..."      # 좋다
```

긴 출력은 `| tail -20`. 큰 결과는 파일로 쓰고 요약만 읽는다.

**③ 알아낸 것은 그 자리에서 적는다.** "나중에 정리"는 없다 — 압축은 아무 때나
온다. 도구를 돌려 얻은 사실은 같은 턴에 `ref/`나 `CLAUDE.md`나 메모로 간다.

**④ 긴 일을 띄우면 즉시 `HANDOFF.md`에 적는다.** 무엇이 돌고, 끝나면 무엇을
할지. 끝나면 지운다. 이것이 없어서 대기 고리 하나가 13시간을 멈춰 있는 동안
아무도 몰랐다.

**⑤ 파일을 통째로 다시 읽지 말 것.** `assistant.html`은 1,100줄이다. `grep -n`
으로 자리를 찾고 그 둘레만 본다. 고칠 때는 파이썬 치환이나 `Edit`으로.

## 2. 결정하기 전에 읽을 것

당구 지식에 대해 무엇이든 정하기 전에, 이미 정해진 것이 있는지 본다. 같은 것을
세 번 다시 발명하고 세 번 다 틀린 적이 있다.

| 무엇 | 어디 |
|---|---|
| 유형을 가르는 기준 | `ref/taxonomy.md` §5 — "이것이 유일한 기준이다"와 **틀렸던 규칙(되풀이하지 말 것)** |
| 두께 · 당점 · 분리각의 정의 | `ref/terms.md` |
| 선수가 영상 보고 붙인 이름 | `data/labels.json` |
| 가상 테이블에서 준 판단 | `data/table_feedback.json` |

**이것들은 규칙이 아니라 채점표다.** 어떤 규칙이든 여기에 대고 점수를 받는다.
선수 본인의 말: *"나라고 당구를 배우는 입장에서 어떻게 규칙이라고 확정을
하겠어."* 그의 습관을 순위에 컴파일해 넣지 않는다.

## 3. 셸에서 되풀이한 실수

**이 셋은 전부 두 번 이상 저질렀다.** 값은 매번 시간이었다.

- **`pkill -f "..."` 금지.** 패턴이 명령을 담고 있는 **자기 셸**을 잡아서 스스로를
  죽인다 (exit 144). `ps -ef | grep "[t]ool_name"`으로 pid를 찾아 `kill <pid>`.
- **`pgrep -f`로 기다리는 고리를 짜지 말 것.** 고리의 명령줄에 그 패턴이 들어
  있으므로 **자기를 발견하고 영원히 기다린다.** 2026-09-19에 13시간 46분을
  그렇게 날렸고, 그동안 새 경기 11개가 받히지 않았다.
- **기다리지 말고 차례로 돌려라.** 단계를 잇는 일은 `tools/keep_up.sh`가 한다.
- 배경 작업은 `nohup ... &`로 띄우고 로그 파일을 본다.

## 4. 파이프라인

```bash
bash tools/keep_up.sh          # 받기 → 스캔 → 뽑기 → 모델자료 → 조언판
bash tools/keep_up.sh --no-collect
```

파이썬은 항상 `~/.venvs/carom/bin/python`. 각 단계는 이미 한 것을 건너뛰므로
언제 다시 돌려도 안전하다. `data/`와 `build/`는 git에 안 올라간다 (`labels.json`
과 `table_feedback.json`만 `git add -f`로 추적).

## 5. 두 벌로 갈라진 것들

같은 모델이 두 언어에 있다. **한쪽만 고치면 조용히 어긋나고, 증상은 "그럴듯한
틀린 궤적" 하나뿐이다.**

| 파이썬 | JS | 붙들어 두는 시험 |
|---|---|---|
| `src/physics/spin.py` | `src/visualization/assistant/sim.js` | `tests/test_sim_js.py` |
| `src/physics/strength.py` 표 | `sim.js`의 `STRENGTH` | `tests/test_strength.py` |

**물리 상수를 고치면 강도 표를 다시 재야 한다** (`strength.measure()`). 같은
이름이 다른 세기를 가리키게 된다.

## 6. 조언판

```bash
~/.venvs/carom/bin/python tools/build_assistant.py
node --check build/sim.js
```

- 원본은 `src/visualization/assistant/`. **스크래치패드에서 작업하지 말 것** —
  세션과 함께 사라지고, 실제로 며칠간 저장소 밖에 있었다.
- **게시 전에 항상 `node --check`.** 페이지는 스크립트 한 덩어리라 괄호 하나가
  전체를 조용히 죽인다. 정의 없이 호출되던 `kissed()` 하나 때문에 모든 배치가
  "득점하는 선을 찾지 못했습니다"로 나온 적이 있다.

## 7. 숫자 없이 "좋아졌다"고 하지 말 것

| 무엇을 고쳤나 | 무엇으로 재나 |
|---|---|
| 순위 | `tools/table_check.py` (선수 판단), `tools/learn_choices.py` (프로 선택) |
| 유형 규칙 | `tools/rules_check.py` |
| 물리 | `tools/fit_spin.py`, `tools/validate_simulator.py` |
| 좌우 회전 부호 | `tools/check_side_sign.py`, `tools/check_side_vs_circuit.py` |

측정에 쓰는 자를 맞춘 값으로 만들지 말 것. 한 번 이동거리에 맞춰 감쇠곡선을
다시 잡았다가 테이블이 2.5배 느려진 적이 있다 — 영상 창이 닫힐 때 아직 구르던
공을 "다 간 거리"로 읽은 탓이었다.
