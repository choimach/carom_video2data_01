# 이 저장소에서 일하는 법

세션이 바뀌면 대화는 사라진다. 여기 적힌 것만 남는다. **고치기 전에 읽어라.**

## 0. 목적 (규칙)

> 프로 당구 선수들의 실전 데이터를 모아서, 내가 실전에서 마주친 공 배치에 대해
> 최선의 공략 방법을 trajectory와 각 공의 움직임을 통해 보여주는 것.

똑같은 배치는 데이터에 있을 수 없다. **가장 비슷한 배치를 찾아 추론한다** —
지식은 이웃에게서, 궤적은 이 배치에서. 자세한 것은 `PURPOSE.md`.

★**무엇이 먼저인지도 그가 정했다 (2026-09-21): 1 > 2 >> 3 > 4 >> 5.**
① 프로라면 무엇을 골랐을까 → ② 수구의 진행 방향 → ③ 적구들의 진행 →
④ 두께·강도·당점 → ⑤ **경로 이름**. 이름이 **맨 끝**이다.
그런데 지금 코드는 이름을 길목으로 쓴다 (후보를 `유형|1적구|면`으로 묶고 이웃
투표도 유형으로 한다). 그래서 1번이 9지선다 분류가 되어 있고 실측 40%다 —
최빈값만 찍어도 30.8%인 문제다. **1번은 "어떤 샷이냐"이지 "무슨 유형이냐"가
아니다.**

## 1. 토큰을 쓰는 순서

이 프로젝트에서 토큰을 가장 많이 먹은 것은 당구도 물리도 아니었다. **같은 것을
다시 알아내는 일**이었다 — 무엇이 돌고 있나, 어디까지 됐나, 저건 왜 저렇게
했더라. 세션이 압축되면 답이 사라지므로 또 알아냈다.

**① 세션을 열면 한 번:**

```bash
bash tools/state.sh        # 돌고 있는 것 · 단계별 개수 · 채점표 · 진행 중인 일
```

스무 줄이면 끝난다. 이걸 안 하면 같은 것을 알아내는 데 호출 예닐곱 번이 든다.

**② 데이터를 맥락에 쏟지 말 것.** `model.json`은 1,500판이고 `app-data.json`은
840 KB다. 읽지 말고 **물어라**:

```bash
~/.venvs/carom/bin/python -c "...집계만 출력..."      # 좋다
```

긴 출력은 `| tail -20`. 큰 결과는 파일로 쓰고 요약만 읽는다.

**③ 알아낸 것은 그 자리에서 적는다.** "나중에 정리"는 없다 — 압축은 아무 때나
온다. 도구를 돌려 얻은 사실은 같은 턴에 `ref/`나 `CLAUDE.md`나 메모로 간다.

**④ 긴 일을 띄우면 즉시 `HANDOFF.md`에 적는다.** 무엇이 돌고, 끝나면 무엇을
할지. 끝나면 지운다. 이것이 없어서 대기 고리 하나가 13시간을 멈춰 있는 동안
아무도 몰랐다.

**⑤ 파일을 통째로 다시 읽지 말 것.** `assistant.html`은 1,100줄이다. `grep -n`
으로 자리를 찾고 그 둘레만 본다. 고칠 때는 파이썬 치환이나 `Edit`으로.

## 2. 결정하기 전에 읽을 것

당구 지식에 대해 무엇이든 정하기 전에, 이미 정해진 것이 있는지 본다. 같은 것을
세 번 다시 발명하고 세 번 다 틀린 적이 있다.

| 무엇 | 어디 |
|---|---|
| 유형을 가르는 기준 | `ref/taxonomy.md` §5 — "이것이 유일한 기준이다"와 **틀렸던 규칙(되풀이하지 말 것)** |
| 두께 · 당점 · 분리각의 정의 | `ref/terms.md` |
| 선수가 영상 보고 붙인 이름 | `data/labels.json` |
| 가상 테이블에서 준 판단 | `data/table_feedback.json` |
| 몇 판이 필요한가 | `tools/learning_curve.py` — 추정표와 실측 |
| 왜 이웃 찾기가 막혔나 | `HANDOFF.md`의 "차원의 문제였다" — **자료도 특징도 답이 아니다** |
| 남들은 어떻게 풀었나 | `ref/prior_art.md` — 당구 쪽엔 선례가 없다. **체스 국면 검색**이 우리 자리를 지나갔다 |

★**내가 만든 서랍에 없다고 "기록에 없다"고 말하지 말 것.** 대화 원본이
`~/.claude/projects/-mnt-d-Data-Billiard-carom-video2data-01/*.jsonl`에 남아 있다.
2026-09-21에 "1,500/5,000/10,000이라고 했었냐"는 물음에 메모와 `ref/`만 뒤지고
"없다"고 답했는데, 원본에는 표까지 그대로 있었다. **`grep`으로 원본을 먼저 본다.**

**이것들은 규칙이 아니라 채점표다.** 어떤 규칙이든 여기에 대고 점수를 받는다.
그의 습관을 순위에 컴파일해 넣지 않는다.

★**그는 프로 선수가 아니라 당구를 배우는 사람이다.** 본인의 말 (2026-09-20):
*"나는 선수가 아니고 당구를 배우고 있는 일반인이야. 내가 말한 정의도 100
옳다고 믿지 말아줘."* 그리고 전에: *"나라고 당구를 배우는 입장에서 어떻게
규칙이라고 확정을 하겠어."*

그러므로 그가 준 정의는 **가설이다** — 내가 세운 가설보다 훨씬 나은 가설이지만
(실전 감각에서 나왔고, 실제로 좌우 회전 부호와 분리각 버그를 먼저 잡아냈다)
확정된 사실은 아니다. 다룰 때:

- 그의 말을 코드 주석에 "정의 그대로"라고 적지 않는다. **출처와 날짜**를 적고,
  가설인지 대조된 것인지 구분한다.
- 가능하면 **바깥 자료와 데이터 양쪽에 대 본다.** 그가 직접 그러기를 청했다:
  *"횡단샷의 정의는 우선 웹서치를 해서 알려줘."*
- 그렇다고 채점표를 버리는 것은 아니다. 영상 라벨은 여전히 가진 것 중 가장
  나은 기준이고, 그가 틀릴 수 있다는 것은 **더 재라는 뜻이지 무시하라는 뜻이
  아니다.**

## 3. 셸에서 되풀이한 실수

**이 셋은 전부 두 번 이상 저질렀다.** 값은 매번 시간이었다.

- **`pkill -f "..."` 금지.** 패턴이 명령을 담고 있는 **자기 셸**을 잡아서 스스로를
  죽인다 (exit 144). `ps -ef | grep "[t]ool_name"`으로 pid를 찾아 `kill <pid>`.
- **`pgrep -f`로 기다리는 고리를 짜지 말 것.** 고리의 명령줄에 그 패턴이 들어
  있으므로 **자기를 발견하고 영원히 기다린다.** 2026-09-19에 13시간 46분을
  그렇게 날렸고, 그동안 새 경기 11개가 받히지 않았다.
- **기다리지 말고 차례로 돌려라.** 단계를 잇는 일은 `tools/keep_up.sh`가 한다.
- 배경 작업은 `nohup ... &`로 띄우고 로그 파일을 본다.

## 3.5 이 기계에는 코어가 16개다 — 한 개만 쓰지 말 것

2026-09-25에 열거를 **한 코어로 여섯 시간** 돌렸다. 그날 그 여섯 시간을 두 번
썼고 한 번은 값을 안 보고 걸어서 통째로 버렸다. 시뮬레이터는 판마다 완전히
독립이므로 나누는 데 아무 조건이 없었다.

```bash
bash tools/enumerate_all.sh        # 코어 수 − 2 만큼 나눠 돌리고 합친다
```

**GPU(RTX 3050)는 이 일에 맞지 않는다.** 시뮬레이터는 충돌 판정과 사건 순서가
섞인 분기 많은 순차 계산이라 행렬 연산이 아니다. `torch 2.6.0+cu124`가 설치돼
있고 CUDA도 잡히지만 `src/` 어디서도 쓰지 않는다 — 쓸 자리가 있다면 스캔의
공 검출 쪽이지 시뮬레이터가 아니다.

★**몇 시간짜리 일을 걸기 전에 `nproc`을 보고 나눌 수 있는지 먼저 묻는다.**

## 4. 파이프라인

```bash
bash tools/keep_up.sh          # 받기 → 스캔 → 뽑기 → 모델자료 → 조언판
bash tools/keep_up.sh --no-collect
```

파이썬은 항상 `~/.venvs/carom/bin/python`. 각 단계는 이미 한 것을 건너뛰므로
언제 다시 돌려도 안전하다. `data/`와 `build/`는 git에 안 올라간다 —
`labels.json`, `table_feedback.json`, **`videos.json`** 만 `git add -f`로 추적.

**스캔이 끝난 영상은 지운다.** 경기당 7~15 GB이고 스캔 결과는 1 MB도 안 되므로
들고 있을 이유가 없다. 선수가 처음부터 정한 방식이다: *"완료된 경기 video는
url정보만 남기고 지우면 된다."*

```bash
~/.venvs/carom/bin/python tools/retire_videos.py            # 말만 한다
~/.venvs/carom/bin/python tools/retire_videos.py --delete
```

★**주소를 먼저 적고 그 다음에 지운다.** URL은 `data/_screening.json`에만 있었고
`data/`는 git에 안 올라가므로, 그대로 지웠으면 **영상을 영영 잃을 뻔했다.** 이
도구가 `data/videos.json`에 옮겨 적은 뒤에만 지운다. 스캔이 안 된 영상과 주소를
모르는 영상은 건드리지 않는다.

## 5. 두 벌로 갈라진 것들

같은 모델이 두 언어에 있다. **한쪽만 고치면 조용히 어긋나고, 증상은 "그럴듯한
틀린 궤적" 하나뿐이다.**

| 파이썬 | JS | 붙들어 두는 시험 |
|---|---|---|
| `src/physics/spin.py` | `src/visualization/assistant/sim.js` | `tests/test_sim_js.py` |
| `src/physics/strength.py` 표 | `sim.js`의 `STRENGTH` | `tests/test_strength.py` |
| `src/physics/route.py` | `route.js` | `tests/test_route_js.py` |
| `tools/learn_choices.py`의 `features()` | `choice.js` | `tests/test_choice_js.py` |

**물리 상수를 고치면 강도 표를 다시 재야 한다** (`strength.measure()`). 같은
이름이 다른 세기를 가리키게 된다.

## 6. 조언판

```bash
~/.venvs/carom/bin/python tools/build_assistant.py
node --check build/sim.js
```

- 원본은 `src/visualization/assistant/`. **스크래치패드에서 작업하지 말 것** —
  세션과 함께 사라지고, 실제로 며칠간 저장소 밖에 있었다.
- **게시 전에 항상 `node --check`.** 페이지는 스크립트 한 덩어리라 괄호 하나가
  전체를 조용히 죽인다. 정의 없이 호출되던 `kissed()` 하나 때문에 모든 배치가
  "득점하는 선을 찾지 못했습니다"로 나온 적이 있다.

## 7. 숫자 없이 "좋아졌다"고 하지 말 것

| 무엇을 고쳤나 | 무엇으로 재나 |
|---|---|
| 순위 | `tools/table_check.py` (선수 판단), `tools/learn_choices.py` (프로 선택) |
| 유형 규칙 | `tools/rules_check.py` |
| 물리 | `tools/fit_spin.py`, `tools/validate_simulator.py` |
| 좌우 회전 부호 | `tools/check_side_sign.py`, `tools/check_side_vs_circuit.py` |

측정에 쓰는 자를 맞춘 값으로 만들지 말 것. 한 번 이동거리에 맞춰 감쇠곡선을
다시 잡았다가 테이블이 2.5배 느려진 적이 있다 — 영상 창이 닫힐 때 아직 구르던
공을 "다 간 거리"로 읽은 탓이었다.

★**고친 쪽만 재지 말고 나빠진 쪽도 재라.** 전방위 훑기를 넣어 걸어치기 재현율을
0/7 → 6/6으로 고치고 **그것만** 확인한 채 올렸는데, 같은 변경이 조언판 실행을
2.6초에서 **18.1초**로 만들었다. 이틀 뒤 다른 도구가 "배치당 20초"를 찍어서야
알았다. 변경마다 **얻은 것과 잃은 것을 같이 적는다.**

★**긴 작업을 걸기 전에 결과 **값**을 눈으로 본다.** "돌아간다"는 확인이 아니다.
2026-09-24에 열거를 여섯 시간 돌리고 나서야 **여유가 2,444판 전부 0**인 것을
알았다 — 열거 쪽 hit에는 `speed`가 없고 `strength`만 있어서 다시 치는 함수가
undefined를 받았다. 걸기 전에 한 판만 돌려 **숫자를 찍어 봤으면** 1분에 잡혔다.
작은 표본으로 먼저 돌리고, 나온 값이 그럴듯한지 본 다음에 건다. 오래 도는
도구에는 **일찍 멈추는 안전장치**를 둔다 (`enumerate_alternatives.js`의
"열 판 내리 여유가 전부 0이면 멈춘다").

★**배운 가중치는 손으로 베끼지 않는다.** `tools/build_assistant.py`가
`data/choice_weights.json`에서 넣는다. 베끼면 다시 재고도 옛 숫자를 내보낸 채
"좋아졌다"고 말하게 된다.
