# 진행 중인 일

세션은 끝나고 대화는 압축된다. **몇 시간짜리 일을 띄우면 그 즉시 여기 적는다** —
무엇이 돌고 있는지, 끝나면 무엇을 해야 하는지. 적지 않으면 다음 세션은 그것이
있는 줄도 모르고, 실제로 대기 고리 하나가 13시간을 멈춰 있는 동안 아무도
몰랐다.

끝난 일은 지운다. 이 파일은 기록이 아니라 **지금 떠 있는 것의 목록**이다.
지난 일은 git 로그와 `ref/`에 있다.

`bash tools/state.sh`가 이 파일의 아래 절을 함께 보여준다.

## 지금 돌고 있는 것 (2026-09-20 새벽)

- **`bash tools/keep_up.sh`** — 받기 → 스캔 → 뽑기 → 모델자료 → 조언판.
  스크리닝을 통과한 새 경기 **11개**(LIWC2026·AKR2026·HCMC2026·T3WC2026·
  SHWC2025·GWWC2025·ANWC2025·POWC2025·AKR2025·HCMC2025·BOWC2025)를 받는 중.
  약 90 GB, 스캔까지 밤새 걸린다. 로그 `data/_keep_up.log`.
  **끝나면**: 영상 34→45, 스캔 32→43, 플레이가 얼마나 늘었는지 보고.

- **`node tools/enumerate_alternatives.js`** — 프로 배치 1,529개에 탐색을 돌려
  **버린 길**을 만드는 중. 약 90분. 로그 `data/_alternatives.log`,
  결과 `data/alternatives.jsonl` (한 줄에 한 판, 이어받기 됨).
  **끝나면**: `tools/learn_choices.py`를 돌려 세 순위를 비교한다 —
  아무렇게나 / 지금의 손 식 / 배운 식. **그 손 식은 한 번도 측정된 적이 없다.**
  그리고 `reached`가 아닌 판이 어느 유형에 몰려 있는지 다시 센다.

## 끝나면 선수와 같이 할 일

- 탐색이 재현하지 못한 프로 플레이들. 선수가 직접 보고 찾아 주기로 했다
  ("못찾은것들은 내가 찾아줄께. 나중에 나랑 같이 찾아").
  **먼저 유형별로 몰려 있는지 세어 본다** — 지난번엔 걸어치기 0/7이어서 그건
  탐색의 구멍이었고, 전방위 훑기를 더해 6/6이 되었다. 흩어져 있을 때만
  선수의 시간을 쓴다.
