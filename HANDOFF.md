# 진행 중인 일

세션은 끝나고 대화는 압축된다. **몇 시간짜리 일을 띄우면 그 즉시 여기 적는다** —
무엇이 돌고 있는지, 끝나면 무엇을 해야 하는지. 적지 않으면 다음 세션은 그것이
있는 줄도 모르고, 실제로 대기 고리 하나가 13시간을 멈춰 있는 동안 아무도
몰랐다.

끝난 일은 지운다. 이 파일은 기록이 아니라 **지금 떠 있는 것의 목록**이다.
지난 일은 git 로그와 `ref/`에 있다.

`bash tools/state.sh`가 이 파일의 아래 절을 함께 보여준다.

## 지금 돌고 있는 것 (2026-09-20 오전)

- **`bash tools/keep_up.sh`** — 2/5 스캔. 34/41, **7개 남음**, 경기당 26~39분이라
  **오후 3시쯤** 끝난다. 끝나면 3~5단계(뽑기·모델자료·조언판)가 자동으로 이어진다.
  로그 `data/_keep_up.log`.
  - 감시가 걸려 있다: 로그에 `=== 끝 ===`이나 `!! `가 나타나면 알림이 온다.
    **`pgrep` 대기 고리는 쓰지 않았다** — 그걸로 13시간을 날린 적이 있다.
  - ⚠️ 새 경기의 **당구대 노출률이 낮다** (BOWC2025 34%, HCMC2025 27%). 포항 98%,
    앙카라 59%와 비교된다. 경기 수는 늘어도 **플레이 수는 비례해 늘지 않는다.**
  - 내려받기는 06:53에 끝났고 11개 중 7개가 들어왔다. 실패한 넷(SHWC2025
    180515335, GWWC2025 177251523, ANWC2025 174676227, POWC2025 165143401) —
    로그에 "error reading header"가 있으니 디코딩 확인에서 떨어진 쪽이다.

- **`node tools/enumerate_alternatives.js`** — ⚠️ **이 결과는 버린다.** 도는 중에
  `route.js`를 한 벌로 합쳤으므로 옛 이름표를 쓰고 있다.

## 파이프라인이 끝나면 (순서대로)

1. `rm data/alternatives.jsonl` 후 `node tools/enumerate_alternatives.js` 다시
   — 새 플레이 + 합쳐진 이름표 + 새 꼬리표(리버스·세워치기·더블). 약 2시간.
2. `~/.venvs/carom/bin/python tools/learn_choices.py` — 교차검증으로 다시 재고,
   **배운 식을 조언판 순위에 넣을지 결정**한다.
3. 재현 실패를 유형별로 센다. 몰려 있으면 내 탐색의 구멍이고, 흩어져 있으면
   그때가 그와 함께 볼 자리다. **맛세로 친 플레이는 원리상 못 찾는다**는 것을
   먼저 떠올릴 것 (`ref/terms.md`).

## 어제 잰 것 (1,096판, 경기 단위 10겹 교차검증)

| | 1등 | 3등 안 | 자리 중앙값 |
|---|---|---|---|
| 아무렇게나 | 5% | 16% | 10.0 |
| 손 식 (지금 앱) | 13% | 33% | 6.0 |
| **배운 식** | **22%** | **46%** | **4.0** |

짝지은 부호검정 7.5 표준편차. 가중치: 두께 +2.00 · 줄두께 +1.41 · 세기 −1.31 ·
쿠션수 −1.28 · 여유 +0.74 · 상단당점 +0.42 — **있는 것 중 가장 단순한 길**.

## 물어볼 것이 남은 것

- **횡단 vs 대회전**: 다섯 번 오간 것이 어느 쪽인가. 물었더니 *"잘 모르겠어"*.
  장-장-장-장-장 플레이에 이름을 붙여 주면 갈린다.
