# 진행 중인 일

세션은 끝나고 대화는 압축된다. **몇 시간짜리 일을 띄우면 그 즉시 여기 적는다** —
무엇이 돌고 있는지, 끝나면 무엇을 해야 하는지. 적지 않으면 다음 세션은 그것이
있는 줄도 모르고, 실제로 대기 고리 하나가 13시간을 멈춰 있는 동안 아무도
몰랐다.

끝난 일은 지운다. 이 파일은 기록이 아니라 **지금 떠 있는 것의 목록**이다.
지난 일은 git 로그와 `ref/`에 있다.

`bash tools/state.sh`가 이 파일의 아래 절을 함께 보여준다.

## 지금 돌고 있는 것 (2026-09-20 오전)

- **`bash tools/keep_up.sh`** — 2/5 스캔 단계. 새 경기 **9개**를 스캔 중,
  경기당 약 40분이라 **오후 1~2시**쯤 끝난다. 로그 `data/_keep_up.log`.
  내려받기는 06:53에 끝났고 **11개 중 7개**가 들어왔다 — 실패한 넷
  (SHWC2025 180515335, GWWC2025 177251523, ANWC2025 174676227,
  POWC2025 165143401)은 왜 빠졌는지 확인해야 한다.

- **`node tools/enumerate_alternatives.js`** — 곧 끝난다 (1,529 중 1,300쯤).
  ⚠️ **이 결과는 버린다.** 도는 중에 `route.js`를 한 벌로 합쳤으므로 지금
  돌고 있는 프로세스는 **옛 이름표**를 쓰고 있다 (되돌아오기·횡단이 안 나온다).

## 스캔이 끝나면 (한 번에)

1. `keep_up.sh`가 3~5단계를 알아서 돌린다 → `model.json`, `build/app-data.json` 갱신
2. `rm data/alternatives.jsonl` 후 `node tools/enumerate_alternatives.js` **다시**
   — 새 플레이와 합쳐진 이름표를 함께 반영. 약 2시간.
3. `~/.venvs/carom/bin/python tools/learn_choices.py` — 교차검증으로 다시 재고,
   **배운 식을 조언판 순위에 넣을지 결정**한다.

## 오늘 아침까지 잰 것 (1,096판, 경기 단위 10겹 교차검증)

| | 1등 | 3등 안 | 자리 중앙값 |
|---|---|---|---|
| 아무렇게나 | 5% | 16% | 10.0 |
| 손 식 (지금 앱) | 13% | 33% | 6.0 |
| **배운 식** | **22%** | **46%** | **4.0** |

짝지은 부호검정 7.5 표준편차 — 차이라고 불러도 된다.
가중치: 두께 +2.00 · 줄두께 +1.41 · 세기 −1.31 · 쿠션수 −1.28 · 여유 +0.74 ·
상단당점 +0.42. **프로는 있는 것 중 가장 단순한 길을 고른다** — 두껍게, 약하게,
쿠션 적게.

## 끝나면 선수와 같이 할 일

- **횡단의 가르는 정의**를 여쭤야 한다. `ref/taxonomy.md`에는 실행 요령만 있고
  (기울기·두께) 무엇이 횡단인지가 없다. 지금 코드는 "단쿠션 없이 두 장쿠션
  사이를 건너다닌다"로 두었는데 **선수에게 확인받은 적이 없다.**
- 탐색이 재현하지 못한 프로 플레이 (지금 11%). 유형별로 몰려 있는지 먼저 세고,
  흩어져 있을 때만 선수의 시간을 쓴다.
