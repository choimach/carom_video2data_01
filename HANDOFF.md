# 진행 중인 일

세션은 끝나고 대화는 압축된다. **몇 시간짜리 일을 띄우면 그 즉시 여기 적는다** —
무엇이 돌고 있는지, 끝나면 무엇을 해야 하는지. 적지 않으면 다음 세션은 그것이
있는 줄도 모르고, 실제로 대기 고리 하나가 13시간을 멈춰 있는 동안 아무도
몰랐다.

끝난 일은 지운다. 이 파일은 기록이 아니라 **지금 떠 있는 것의 목록**이다.
지난 일은 git 로그와 `ref/`에 있다.

`bash tools/state.sh`가 이 파일의 아래 절을 함께 보여준다.

## 지금 돌고 있는 것

**`tools/enumerate_alternatives.js` 전체 재실행** (2026-09-24 시작).
로그 `data/_alternatives.log`. 옛 파일은 `data/alternatives_coarse.jsonl.bak`
(1,801판, 굵은 눈금·중앙값 대표).

**왜 다시 도나 — 세 가지가 한꺼번에 바뀌었다:**
1. **여유를 이분법으로 잰다.** 0.25도 눈금에서는 통의 **95%가 같은 값에
   묶였다** (프로 배치 20개, 통 410개). 실제 창은 중앙값 0.15도에 가장 넓은
   것이 3.15도 — 21배 차이가 실재하는데 눈금이 못 보고 있었다. 0.05도 칸
   세기로도 74%가 묶여서, 가장자리를 배로 늘려 가둔 뒤 여덟 번 접는다
   (0.006도, 서른 번 남짓).
2. **대표가 실제 줄 하나가 된다.** 항목마다 중앙값을 따로 잡던 것을 버렸다 —
   당점 18%가 아무도 치지 않는 점이었다.
3. **늘어난 643판**이 들어온다 (대상 2,444판).

**끝나면 할 일 (차례대로):**
- `~/.venvs/carom/bin/python tools/learn_choices.py` — 가중치 다시 맞추기.
  눈금이 바뀌었으므로 `여유` 가중치는 반드시 다시 재야 한다.
- `~/.venvs/carom/bin/python tools/test_route_prior.py` — 유형빈도가 여전히
  버는지 다시 확인 (전에는 4.1 표준편차).
- `~/.venvs/carom/bin/python tools/build_assistant.py` → 게시.

## 어젯밤 작업이 끝났다 (2026-09-24 06:04)

`data/_keep_up.log`. 자료가 늘었다: **모델 2,162 → 2,466판**, 조언판
1,897 → **2,176판**. 지금 돌고 있는 것은 없다.

⚠️ **`data/alternatives.jsonl`은 아직 1,801판짜리다.** 오늘 맞춘 가중치는 그
자료에 대해서는 맞지만, 늘어난 665판은 아직 "버린 길"이 없다. 아래 ①번 일과
함께 열거를 다시 돌리면 한 번에 따라온다.

## 1쿠션 지점을 끌 수 있다 (2026-09-24, v46)

선수: *"뱅크샷을 칠 경우를 말하는 거야. 이때는 수구와 두께가 없으니까 스트로크
강도, 당점이 정해진 상태에서 1쿠션 지점을 바꾸면 그에 따라 궤적이 바뀔 수 있지?"*

그렇다. **쿠션을 먼저 맞는 공략(뱅크샷·걸어치기)에서만** 손잡이를 단다. 1적구를
먼저 맞는 공략에서는 강도·당점·두께가 정해지면 1쿠션 지점이 이미 정해져 움직일
수 없다.

겨냥은 **기하로 바로 푼다** — 수구는 첫 쿠션까지 곧게 가므로 끄는 자리를 향한
각도가 곧 답이다. 1쿠션이 오차 **1 mm 안**으로 따라가고 한 번에 2.5 ms다.
한때 각도를 훑어 찾게 짰다가 버렸다: ±2.5도만 보아 1쿠션이 80~160 mm밖에 안
움직였는데, 600 mm를 옮기려면 22도가 필요했다.

끌다 득점을 잃으면 **선 색이 바뀌고** 글자가 "안 들어감"으로 바뀐다 — 창이
얼마나 좁은지 손으로 느끼는 것이 이 기능의 값어치다. 두 번 누르면 원래대로.

## 2,466판 중 모델이 실제로 배운 것은 1,580판이다 (2026-09-24)

선수: *"2176판이 모두 model 작업에 사용되었어?"* 아니다.

| 단계 | 판 수 | 무엇에 쓰이나 |
|---|---|---|
| `model.json` | **2,466** | 영상에서 뽑은 전부 |
| `app-data.json` | **2,176** | 조언판의 이웃 풀 |
| `alternatives.jsonl` | **1,801** | 버린 길까지 세운 판 |
| └ 탐색이 프로의 길을 찾은 것 | **1,580** | 순위 가중치가 배운 판 |

세 군데서 샌다.

★**① 2,466 → 2,176: `route_model.py`의 `OUTCOME_IN_NAME = ("대회전",
"되돌아오기", "횡단")`이 이웃 풀에서 통째로 뺀다** — 대회전 254 · 되돌아오기
21 · 횡단 15 = 290판.

이것을 2026-09-23에 모르고 지나쳤다. "횡단이 프로의 0.6%"라고 재고 유형빈도
항이 횡단을 3등 안 0.6% → 0.0%로 눌렀다고 적었는데, **세 유형은 애초에 이웃
투표에서 빠져 있어 프로 수가 늘 0이었다.** 이름에 결과가 들어 있다는 이유로
뺀 것인데 **대회전은 프로가 10.7%나 치는 흔한 공략**이라 빼둘 이유가 약하다.
첫 무작위 판에서 대회전 후보가 프로 0/40으로 나온 것도 이것이다.

**결정이 필요하다:** 대회전(그리고 되돌아오기·횡단)을 이웃 풀에 넣을지. 넣고
`tools/ceiling.py`와 `learn_choices.py`를 다시 재서 나아지는지 본다.

**② 2,444 → 1,801 (643판):** 어젯밤 늘어난 분, 열거를 아직 안 돌렸다.

**③ 1,801 → 1,580 (221판):** 탐색이 프로의 길을 못 찾은 판(재현율 88%).
이건 빼는 것이 맞다 — 우리가 놓친 것이지 프로가 버린 것이 아니다.

## 다음에 할 일 ①: 여유를 0.05도로 잰다 (2026-09-24 이어서)

첫 무작위 판(씨앗 1581786015)이 이걸 드러냈다. 후보 12개 중 **11개가 여유
0.25도** — 바닥값이다. **0.25도는 "좁다"가 아니라 "우리 눈금이 거기까지밖에
못 본다"는 뜻이다.** 같은 줄을 0.05도로 다시 재면:

| 후보 | 화면의 여유 | 진짜 창 |
|---|---|---|
| 뱅크샷·빨강 왼쪽 (1위) | 0.5° | 0.15° |
| 뱅크샷·흰공 오른쪽 (2위) | 0.25° | 0.15° |
| 옆돌리기·흰공 왼쪽 | 0.25° | 0.05° |
| 횡단·흰공 왼쪽 (그가 민 것) | 0.25° | **0.00°** |

전체로도 **통 35,742개 중 33%가 0.25도에 묶여 있다.** 가장 무겁게 쓰여야 할
"얼마나 쉬운가"가 세 번에 한 번은 아무 말도 못 한다.

**할 일:** ① 프로 배치 20개로 "세밀하게 재면 묶인 것이 갈리는가"를 먼저 잰다
(`scratchpad/roomtest.js`에 짜 두었으나 돌리다 멈췄다 — 저장소로 옮길 것).
② 갈리면 `enumerate_alternatives.js`와 `finish()` 양쪽에서 대표 줄의 여유를
0.05도로 재고, 열거를 다시 돌린 뒤(약 1시간) 가중치를 다시 맞춘다. 눈금이
바뀌면 `여유`(+0.43)의 분포가 바뀌므로 **반드시 다시 맞춰야 한다.**

⚠️ 곁들여 나온 것: 이 판에서 흰공을 먼저 맞고 득점하는 조합 374가지를 전부
세웠더니 **가장 넓은 창이 0.10도**였다 (빗겨치기·대회전·옆돌리기). 선수가 민
횡단은 0.05도로 그중에서도 가장 좁다. 우리 물리에서는 이 배치에 성립하는
공략이 사실상 없다는 뜻인데, 그건 물리 쪽을 한 번 더 볼 이유이기도 하다.

## 배치는 이제 무작위로 깐다 (2026-09-23)

선수: *"지금까지는 공배치를 내가 정했기 때문에 내가 익숙한 배치만 놓았어.
사실은 공 배치와 수구 선택을 claude가 random하게 한 다음 model을 적용해 보고
그 결과에 대한 내 의견을 적어주는게 맞는 approach라 생각해."*

맞는 말이다 — 채점표 여섯 판이 전부 그가 놓은 배치라, **낯선 배치에서 어떤지는
한 번도 잰 적이 없다.** 조언판에 `무작위 배치` 단추를 붙였다. 세 공을 고르게
뿌리고 **수구도 뽑고** 조건(1적구·면)을 자동으로 푼 뒤 바로 실행한다. 씨앗을
기록에 담으므로 같은 판을 다시 만들 수 있고, 복사한 기록에 `origin`/`seed`가
들어가 손으로 놓은 판과 구별된다. 공을 손으로 옮기면 씨앗 표시는 지워진다.

프로 배치에서 뽑는 방법은 쓰지 않았다: 그 판이 **자기 자신을 이웃으로 찾아**
성적을 부풀리고(같은 경기를 빼야 한다), 프로가 마주친 배치만 보게 되어 또 한 번
치우친다.

무작위 배치 40개로 재 보니 **쓸 만하다**:

| | |
|---|---|
| 답을 하나도 못 찾은 판 | **0개 (0%)** |
| 후보 수 중앙값 | 21개 |
| 걸린 시간 중앙값 | 8.3초 (가장 오래 11.9초) |

★그리고 무작위 배치에서 나온 유형 치우침이 프로 배치에서 잰 것과 거의 같았다
(뱅크샷 18.9% · 걸어치기 18.6% · 횡단 10.8% · 뒤돌리기 9.9% · 옆돌리기 8.7%).
**치우침은 배치 탓이 아니라 탐색 탓이다.**

## 후보 목록이 프로와 다른 모양이다 (2026-09-23)

선수: *"뱅크샷 이런 선택은 안함."* 그 판의 후보 12개 중 넷이 뱅크샷이었다.
프로 2,162판과 우리 후보 35,742개를 견주면:

| 유형 | 프로가 친 것 | 우리 후보 | |
|---|---|---|---|
| 횡단 | 0.6% | 10.7% | **17.8배** |
| 걸어치기 | 7.0% | 17.9% | 2.5배 |
| 뱅크샷 | 12.4% | 19.9% | 1.6배 |
| 대회전 | 10.7% | 14.0% | 1.3배 |
| 빗겨치기 | 8.6% | 8.5% | 1.0배 |
| 앞돌리기 | 10.1% | 9.0% | 0.9배 |
| **뒤돌리기** | **26.2%** | 10.7% | **0.4배** |
| **옆돌리기** | **23.4%** | 9.0% | **0.4배** |
| 되돌아오기 | 1.0% | 0.3% | 0.3배 |

★**프로가 치는 절반(뒤돌리기 + 옆돌리기)이 후보에서는 20%뿐이다.** 전방위
훑기가 쿠션 먼저 맞는 길을 잘 잡는 대가다.

순위에 `유형빈도`(그 유형의 프로 기본 비율, 로그)를 배우는 항으로 넣었다.
이웃 프로 수와 겹치지 않는다 — 이웃은 40명뿐이라 후보의 45%가 0이고 기본
비율은 촘촘하다. 가중치 **+0.43**, 1등 33.4% → 34.3% (4.1 표준편차,
`tools/test_route_prior.py`).

3등 안에 드는 유형의 섞임이 프로 쪽으로 갔다:

| 유형 | 프로 | 3등 안 (전) | 3등 안 (후) |
|---|---|---|---|
| 뒤돌리기 | 26.2% | 31.7% | **36.3%** |
| 옆돌리기 | 23.4% | 27.3% | **29.5%** |
| 횡단 | 0.6% | 0.6% | **0.0%** |
| 걸어치기 | 7.0% | 3.7% | 2.1% ⚠️ 더 내려갔다 |
| 뱅크샷 | 12.4% | 13.5% | 13.3% |

⚠️**뱅크샷은 거의 안 움직였고, 애초에 프로 비율(12.4%)과 거의 같았다.**
즉 그가 본 "뱅크샷 넷"은 순위의 문제가 아니라 **후보 목록의 문제**이고,
게다가 **프로는 뱅크샷을 12%나 친다** — 그의 "안 함"은 프로가 아니라 본인
이야기다. ①의 물음이 "프로라면"이므로 이건 고칠 것이 아니라 알려 줄 것이다.
그가 자기 취향을 순위에 넣기를 원하면 그때는 다른 항이 필요하다.

⚠️ 걸어치기가 프로 7.0%인데 3등 안 2.1%로 더 내려갔다 — `쿠션수 −0.70`이
누르는 것으로 보인다. 아직 안 봤다.

## 당점이 10시로 나온 이유 — 동점 처리였다 (2026-09-23)

선수: *"앞돌리기에서의 당점은 10시 방향이 아니라 1시 방향 정도 되어야 함."*

그 배치(흰 829,399 / 노랑 1401,768 / 빨강 1915,321)의 `앞돌리기|빨강|오른쪽면`
통에는 득점하는 줄이 14개 있고, **14개 전부 조준 여유가 0.25도** — 바닥값에서
동점이다. 대표를 고르는 규칙은 `hit.room > kept.room` 하나뿐이라 동점이면
**배열에 먼저 들어온 줄이 이긴다.** 10시가 뽑힌 이유는 그것뿐이었다.

| 당점 | 줄 | 두께 | 강도 | 회전 |
|---|---|---|---|---|
| **1시 2팁** | **3** | 0.12 | 5.5 | 오른쪽 = 도는 쪽 |
| 4시 2팁 | 2 | 0.04 | 5.5 | 오른쪽 |
| 11시 2팁 | 2 | 0.12 | 5.5 | 왼쪽 |
| 10시 2팁 ← 뽑혔던 것 | 1 | 0.35 | 7.0 | 왼쪽 = 반대 |

선수가 말한 1시가 **그 통에서 줄이 가장 많은 당점**인데도 배열 순서에 밀렸다.

`finish()`에 동점을 가르는 두 단계를 넣었다: ① 같은 당점(좌우·상하 **쌍째**로,
극좌표 한 점이므로)으로 득점하는 줄이 많은 쪽 ② 그래도 같으면 도는 쪽으로
회전을 준 줄 — 프로 2,314판 중 **73%**가 그렇게 친다
(`tools/check_side_vs_circuit.py`). ②는 순위에 넣는 것이 아니라 **아무것도
가릴 수 없을 때만** 쓰므로 "경향을 규칙으로 굳히지 않는다"는 원칙은 그대로다.
고친 뒤 그 통의 답은 **1시 2팁**이다.

### 얻은 것과 잃은 것 (프로 배치 80개 · 묶음 1,661개)

| | 지금 | 고친 뒤 | 영상의 프로 |
|---|---|---|---|
| 대표 줄의 회전이 **도는 쪽**인 비율 | 53.8% | **63.8%** | 73% |
| 프로가 고른 통이 **1등** | 32.4% | 32.4% | |
| 프로가 고른 통이 **3등 안** | 62.0% | 60.6% | |

대표가 바뀐 묶음은 **411/1,661 (25%)**. 바뀐 것들의 두께 평균 변화 −0.009,
강도 +0.07 — 쏠림 없이 갈린다.

★**당점은 프로 쪽으로 10점 갔고, 순위는 움직이지 않았다.** 1등은 그대로이고
3등 안이 1.4점 내려갔지만 재현한 판 71개에서 **판 하나 차이**이며, 짝지은
부호검정이 **0.9 표준편차**(올라간 판 11 · 내려간 판 7 · 비긴 판 53)라 우연과
가릴 수 없다.

⚠️ 순위 비교에 쓴 가중치는 **중앙값 대표로 배운 것**이다 (아래 참조). 아래를
고쳐 다시 맞추면 이 숫자도 다시 재야 한다.

### 곁가지로 나온 것 — 학습 자료의 당점 18%는 아무도 치지 않는 점이다

`tools/enumerate_alternatives.js`의 `bucket()`은 대표를 **좌우와 상하의 중앙값을
따로** 잡아 만든다. 당점은 극좌표 한 점인데 두 숫자로 쪼갠 것이라, 통 35,742개
중 **6,305개(18%)**의 팁 크기가 0·1·2·3 어느 것도 아니다 (1.73과 1.41이 대부분 —
한 줄의 좌우와 다른 줄의 상하를 붙여 놓은 값이다). 즉 **조언판은 실제 줄 하나를
대표로 쓰는데 모델은 아무도 치지 않은 당점으로 배웠다.**

영향은 `회전량`(+0.07)과 `상단당점`(+0.32) 두 특징에 그치지만, 두 벌이 서로 다른
규칙을 쓰는 것 자체가 갈라짐이다. **고치려면 열거를 다시 돌리고(약 1시간)
가중치를 다시 맞춰야 한다.**

## 2026-09-21에 알아낸 것 — 병목은 "고르기"다

선수가 차례를 정했다: **1 > 2 >> 3 > 4 >> 5** (프로의 선택 > 수구 진행 > 적구
진행 > 두께·강도·당점 >> 경로 이름). 그리고 화면에는 **궤적 1개(+2개)** 면 된다.

그래서 이름을 거치지 않고 **이웃의 샷 자체를 옮겨** 보았다
(`tools/carry_shot.py` + `.js`). 옮기는 것은 절대 각도가 아니라 관계다 —
가까운 공이냐 먼 공이냐, 어느 면, 몇 두께, 얼마나 세게. 겨냥은 이 배치에서
다시 계산한다. 재는 자는 이름이 아니라 **궤적 거리**다 (①과 ②를 한꺼번에 잰다).

프로 플레이 100개, 같은 경기는 이웃에서 뺐다:

| | |
|---|---|
| 1적구를 맞게 골랐나 | **70%** (우연이면 50%) |
| 1적구 **+ 면**까지 | **35%** (우연이면 25%) |
| **면만 따로** | **50% — 정확히 우연** |
| 궤적 거리 (전체) | 중앙값 616 mm |
| **고르기가 맞았을 때** | **중앙값 273 mm** |
| 고르기가 틀렸을 때 | 중앙값 846 mm |

★**궤적을 만드는 기계는 잘 돈다.** 고르기만 맞으면 프로가 간 길과 273 mm
(공 네 개, 장축의 1/10) 안이다. 616 mm는 전부 고르기가 틀려서 나온 값이다.

★**면은 아무 정보 없이 찍히고 있다.** 그런데 면이 수구가 테이블을 **어느 쪽으로
도는지**를 정한다. ①번의 절반이 동전 던지기였다.

## 궤적을 고리별로 재 봄 (2026-09-21, `tools/carry_shot.js`)

선수가 궤적 안의 차례도 정했다: **반사각 > 첫 쿠션 자리 > 곡선 > 이동거리 >
둘째 쿠션 자리.** 앞의 고리일수록 무겁다 — 오차가 뒤로 갈수록 불어나므로.

고르기가 맞은 31개에 대해:

| 고리 | 오차 (중앙값) | |
|---|---|---|
| 1. 반사각 방향 | **12도** | |
| 2. 첫 쿠션 자리 | **113 mm** | 공 두 개 안 — 좋다 |
| 3. 곡선 (간 거리 ÷ 직선) | 0.29배 | |
| 4. 총 이동거리 | 1178 mm | ⚠️ **믿지 말 것** |
| 5. 둘째 쿠션 자리 | 370 mm | 1번 쿠션의 3배 — 불어난다 |

★**첫 쿠션까지는 쓸 만하다** (113 mm). 둘째에서 370 mm로 벌어진다. 선수가 말한
"오차가 뒤로 갈수록 불어난다"가 숫자로 보인다.

⚠️ **4번은 잴 수 없다.** 영상의 궤적은 카메라 창이 닫히며 잘리고, 추적된 공의
대부분이 그때 아직 구르고 있었다. 끝까지 굴린 시뮬레이터와 견주면 우리가 더 간
것처럼 보일 뿐이다. **이 함정에 한 번 걸려 감쇠곡선을 잘못 맞췄다가 테이블이
2.5배 느려진 적이 있다.** 이동거리를 재려면 다른 자가 필요하다.

★**쿠션 자리는 이제 추측하지 않는다.** `build/app-data.json`이 1,584/1,585
플레이에 대해 **파이프라인이 아는 진짜 쿠션 자리와 1적구 접촉 자리**를 싣는다
(`rails`, `hit`). 그 전에는 32점으로 솎인 길에서 되짚다가 첫 쿠션을 1434 mm
틀리게 읽었다 — 점 간격이 200 mm인데 쿠션 띠가 49 mm였다.

## 프로의 선택 기준 (2026-09-21) — 일의 나눔

**1. 득점 확률 · 2. 키스 · 3. 다음 배치.** 그리고 *"3번은 억지로 계산할 필요
없어 — 프로가 선택한 궤적이 이미 키스를 피하고 다음 공이 좋을 수 있는 선택을
하니까."*

- **1번은 시뮬레이터가** 계산한다. 지금은 "줄두께"로 어림잡고 있는데, 어제
  만들어 둔 흔들림 계산(후보 6개에 294 ms)을 순위에 넣으면 직접 잰다.
- **2·3번은 프로 데이터가** 안고 온다. 따로 계산하지 않는다. 배운 가중치의
  `쿠션수 −1.42 · 세기 −1.21 · 1적구이동 −0.27`이 3번이 남긴 자국이다.

## 상한을 쟀다 (2026-09-23, `tools/ceiling.py`)

①번의 목표가 "확률이 높은 궤적"이므로 채점은 하한만 준다. 그래서 **아무리 잘해도
몇 %인가**를 먼저 쟀다. 플레이 1,887판, 이웃 40명(같은 경기 제외):

| | 최빈값만 | **상한** | 지금 |
|---|---|---|---|
| 1적구 | 62.9% | **64.9%** | 70% (다른 실험값, 직접 비교 주의) |
| **면** | 50.8% | **59.6%** | ~50% |
| 공+면 | 32.2% | **40.1%** | 35% |
| 유형 | 30.0% | **39.4%** | **40%** |

★**유형은 이미 천장이다** (40 vs 39.4). 더 배울 것이 없다.
★**면에 9점, 공+면에 5점이 남아 있다.**
★**상한 자체가 낮다.** 면조차 59.6% — 닮은 배치에서 열 명 중 넷이 반대쪽을 친다.

★★**가장 아픈 것: 가장 가까운 5명이 40명보다 겨우 2점 더 일치한다**
(면 54.4% vs 52.1%). 배치가 더 닮았는데 선택이 더 일치하지 않는다는 것은,
**특징 12개가 선택을 가르는 정보를 안 담고 있다**는 뜻이다. "닮았다"의 기준이
틀린 것이다.

**그러므로 남은 지렛대는 특징이다.** 상한은 "지금 특징으로 정의한 이웃 안에서의
일치도"이므로, 특징이 좋아지면 **상한과 점수가 같이 오른다.**

| 무엇을 고치면 | 얼마나 |
|---|---|
| 자료 더 모으기 | 두 배마다 +2점 |
| 순위 식 고치기 | 100판에서 포화 |
| **특징 고치기** | **상한 자체를 올린다** |

⚠️ 상한은 낙관적으로 잡힌 값이다 — 이웃 40명에서 "최다의 몫"을 쓰므로 표본
흔들림이 최댓값을 부풀린다. 갈래가 많은 유형(9가지)에서 더 그렇다.

## 특징을 갈아 봤지만 벽은 그대로다 (2026-09-23)

상한이 낮은 것이 특징 탓인지 보려고, 옆 프로젝트 `carom_codex`의
`05_similarity_search`를 읽고 거기 있는 것 중 우리에게 없는 넷을 더해 보았다:

- **삼각형의 세 각** — 우리는 수구 쪽 각 하나만 썼고 적구 쪽 둘이 없었다
- **가리는 선** (`_line_clearance`) — 한 적구가 수구→다른 적구 선을 얼마나 막나

결과: **전부 ±0.5점 안, 잡음이다.**

| | 전 | 후 |
|---|---|---|
| 면 상한 | 59.6% | 59.5% |
| 공+면 상한 | 40.1% | 40.1% |
| 유형 상한 | 39.4% | 40.0% |

되돌렸다 — 측정되지 않은 이득에 복잡도를 남기지 않는다.

★**그쪽도 같은 벽에 있다.** `coordinate_v1` 23.5% → `geometry_v2` 26.5% (+3점)
→ `strategy_geometry_v3` 25.8% (−1점). 서로 다른 코드베이스에서 서로 다른 특징으로
**같은 자리에 멈췄다.**

**그러므로 벽은 특징이 아닐 가능성이 크다.** 남는 설명은 하나다 — **선택이 배치
만으로 정해지지 않는다.** 점수 상황, 상대, 선수의 버릇, 그날의 자신감 같은 것이
들어간다. 그것들은 배치에 없다.

★**그러면 답은 하나를 더 잘 맞히는 것이 아니라 확률로 보여주는 것이다.**
선수가 처음부터 그렇게 말했다 (2026-09-21): *"프로가 선택을 할 **확률이 높은**
궤적"*, 그리고 *"궤적 1개를 보여주면 돼. 그 다음 두 개 정도 보여주면 더 좋고."*
3등 안 커버리지가 50%이므로, **1개 + 2개면 절반은 맞는다.**

## 왜 벽이 있나 — 차원의 문제였다 (2026-09-23)

특징 12개를 각 축 퍼짐이 1이 되게 맞춰 놓고 거리를 재 보면:

| | 거리 (표준편차 단위) |
|---|---|
| 아무 두 판 사이 | 4.87 |
| **가장 가까운 이웃** | **1.18** |
| 5번째 이웃 | 1.69 |
| 40번째 이웃 | 2.54 |

★**가장 가까운 이웃이 1.18이다.** 12축에 걸쳐 1.18이면 **축마다 평균 0.34
표준편차**씩 다르다는 뜻이다. 수구-1적구 거리의 퍼짐이 400 mm쯤이면, 가장 닮은
배치조차 1적구가 13 cm 떨어져 있고 나머지 열한 축도 비슷하게 어긋나 있다.
**닮은 배치가 아니라 어렴풋이 비슷한 배치다.**

### 이 하나로 세 가지가 다 설명된다

1. **가장 가까운 5명이 40명보다 안 나은 이유** — 1.69와 2.54는 뜻 있는 차이가
   아니다. 둘 다 "그냥 좀 비슷한" 것이다.
2. **특징을 갈아도 안 움직이는 이유** — 문제는 어느 축을 재느냐가 아니라
   **가까운 예가 아예 없다**는 것이다. 축을 바꿔도 여전히 없다. carom_codex가
   두 번 갈아 보고 같은 자리에 멈춘 것도 이것이다.
3. **자료가 두 배마다 +2점인 이유** — 12차원에서 최근접 거리는 자료 개수의
   **12제곱근**으로만 줄어든다. 거리를 절반으로 줄이려면 **2¹² = 4,096배**가
   필요하다. 1,887 → 10,000판(5.3배)이면 거리는 1.18 → 1.03으로 줄 뿐이다.
   **10,000판을 모아도 "닮은 배치"는 여전히 안 닮았다.**

### 그래서 세 갈래가 다 막힌 것이다

| 시도 | 왜 안 됐나 |
|---|---|
| 자료 모으기 | 12차원을 채우려면 수천 배 — 불가능 |
| 순위 식 고치기 | 100판에서 포화 (변수 9개라 배울 게 없음) |
| 특징 갈아 끼우기 | 축을 바꿔도 가까운 예가 없는 것은 그대로 |

그리고 앞뒤가 맞는다: **순위 식이 100판에서 포화한 것과 kNN이 굶는 것은 같은
이야기의 양면이다.** 변수가 적은 모델은 자료가 조금만 있어도 다 배우고,
12차원 이웃 찾기는 자료가 아무리 있어도 못 채운다.

### 남은 진짜 지렛대: 차원을 줄인다

★당구의 "시스템"들이 정확히 그것을 한다 — 파이브앤하프는 **수구 위치 하나,
목표 지점 하나**, 두세 개 숫자로 길을 정한다. 열두 개가 아니라.

**3~4개 숫자로 배치를 요약할 수 있다면** 1,887판이 그 공간을 빽빽하게 채운다.
그때 비로소 "이 배치에서 프로들은 이렇게 친다"가 성립한다.

어느 3~4개냐는 데이터가 고르게 할 수 있다 (어느 축 조합이 선택을 가장 잘
가르는지). **두 프로젝트 다 안 해 본 것**이고, 위 산수가 그쪽을 가리킨다.

⚠️ 이것이 "선택은 배치만으로 안 정해진다"를 부정하지는 않는다. 둘 다 참일 수
있다. 다만 **진짜로 가까운 배치를 본 적이 한 번도 없으므로 아직 가릴 수 없다.**
차원을 줄여 가까운 이웃을 만든 다음에야 그 질문에 답할 수 있다.

## 차원을 줄여 봤고, 답이 났다 (2026-09-23) — `tools/reduce_dims.py`

12차원에서는 가장 가까운 이웃이 1.18 표준편차 떨어져 있어 "닮은 배치"가 없었다.
그래서 NCA로 12축을 섞어 3~4축으로 내렸다 (경기 단위 분할, 학습에 쓴 경기는
평가에서 뺀다).

**밀도 문제는 풀렸다:**

| | 이웃과 일치 | 최근접 거리 |
|---|---|---|
| 배치와 **무관할 때** | **26.5%** | — |
| 12축 | 27.8% | 1.52 |
| 3축 | 27.8% | **0.25** |
| 4축 | 28.0% | **0.45** |

★**이웃을 3~6배 가깝게 만들었는데 일치도가 0.2점 움직였다.** 그리고 셋 다
"배치와 무관할 때"보다 **1점 남짓** 높을 뿐이다.

★★**그러므로 배치는 프로가 어느 공을 어느 면으로 칠지를 거의 정하지 않는다.**
특징 탓도, 자료 탓도, 차원 탓도 아니었다. **거의 똑같은 배치를 놓고도 프로들은
서로 다르게 친다.**

### 가는 길에 진짜 버그를 하나 잡았다

배치는 캐노니컬 구역으로 **뒤집어** 저장하면서 면(`face`)은 원본 프레임의
`struck_side`를 그대로 쓰고 있었다. **거울 한 번은 손을 바꾸므로**, 거울이 홀수
번 걸린 판 — **48%** — 은 왼쪽 면이 오른쪽 면으로 저장돼 있었다.

고쳤다. 그런데 **신호가 나타나지 않았다.** 오히려 면 일치도가 52.1% → 49.7%로
내려갔다 (최빈값 50.3%). 즉 고치기 전의 52.1%도 이미 아무것도 아니었다.
**버그는 진짜였지만 원인은 아니었다.** 고침은 옳으므로 남긴다 — 거울에 비친
라벨은 점수와 무관하게 틀린 것이다.

### 그래서 무엇을 할 것인가

하나를 더 잘 맞히려는 시도는 여기서 멈춘다. 남은 것은 **확률로 보여주는 것**이고,
선수가 처음부터 그렇게 말했다 (2026-09-21):

> *"프로가 선택을 할 **확률이 높은** 궤적"*, *"궤적 1개를 보여주면 돼.
> 그 다음 두 개 정도 보여주면 더 좋고."*

그리고 **①번이 막혀도 ②③④는 살아 있다.** 고르기가 맞았을 때 첫 쿠션 113 mm,
궤적 273 mm로 그려 준다. 조언판의 값어치는 "프로의 답을 맞히는 것"이 아니라
**"들어가는 길들을 실제로 보여주고, 그중 프로가 자주 고르는 쪽을 위에 놓는 것"**
이다.

⚠️ 남은 단서: 여기서 "배치"는 **정지한 세 공의 위치**다. 프로의 선택에는 점수
상황·상대·자기 버릇·다음 배치 계획이 들어가고, 그것들은 테이블 위에 없다.
그 정보를 데이터에 넣을 길이 생기면 이 결론은 다시 열린다.

## 다음에 할 일 (차례대로)

1. ★**면을 맞히는 것** — 가장 큰 지렛대다. 이웃 40명의 다수결인데 정확히 우연
   (50%)이다. **부호 문제는 아니다** — 2026-09-21에 확인했고 `struck_side`는
   선수의 손 표시 다섯 판에 5/5로 맞다.

   **①번의 목표는 확률이다** (선수, 2026-09-21): *"프로가 실제로 친 게 아니고,
   프로 데이터를 기반으로 임의 배치에서 프로가 선택을 할 **확률이 높은** 궤적."*
   그러므로 지금의 25%는 **하한**이고, 상한은 프로들끼리의 일치도가 정한다.

   **다음 한 걸음: 상한이 얼마인지부터 잰다.** 서로 닮은 배치를 여러 프로가
   쳤을 때 **면이 얼마나 일치하는가**를 세면 갈린다:
   - 많이 일치하면 → 배치가 면을 정한다는 뜻. 특징 12개가 그 정보를 못 담고
     있는 것이고, **고칠 수 있다**.
   - 잘 안 일치하면 → 같은 배치에서도 사람마다 다르게 고른다는 뜻이고,
     **상한이 낮다.** 그러면 면을 맞히려 애쓰는 대신 두 가지를 다 보여주는
     쪽이 맞다 (그가 궤적 1개 + 2개를 원한 것과도 맞는다).

   ⚠️ **면을 따로 예측할 필요가 없다는 것도 2026-09-21에 짚였다.** 그의 말:
   "궤적을 보면 어느 쪽 면을 쳤는지 알 수 있는 건데 왜 미리 예측을 하지?"
   맞다 — 앱은 이미 360도를 훑어 들어가는 선을 다 찾고, **후보마다 자기 면을
   갖고 있다.** 면은 결정이 아니라 결과의 성질이다. `carry_shot` 실험이 면을
   먼저 정하는 병목을 **내가 만들어 넣은 것**이고, "면이 동전 던지기"는 그
   병목에 대한 이야기이지 넘어야 할 벽이 아니다.

   그러니 ①번의 진짜 질문은 **"들어가는 선 스무 개 중 프로가 고를 확률이 가장
   높은 것을 맨 위에 올리는가"**이고, 그 점수가 25%다.
2. **노란공 검출** — `AKR2025` 104판이 전부 `layout_incomplete`, 89판에서
   노란공이 빠졌다. T3WC2026 네 경기도 같은 계열. **5경기 0판**, 고치면 ~100판.
   그리고 스크리닝이 "공이 잡히나"를 안 봐서 앞으로도 계속 걸린다.
3. **당점을 내보내기에 추가** — `app-data.json`에 팁이 없어 ④의 한 조각을
   이웃에게서 옮길 수 없다. 지금은 탐색으로 채운다.
4. 되돌아오기 2/19 · 빗겨치기 75% (재현 실패), 뱅크샷 과대평가, 받다 실패한 4개.

## 재 둔 것들 (다시 재려면)

- `tools/learning_curve.py` — 자료를 늘리면 얼마나 나아지나. **순위 식은 100판에서
  포화**, **이웃 찾기는 두 배마다 +2점** (1,335판에서 40%, 최빈값 30.8%).
- `tools/carry_shot.py` + `.js` — 위의 표.
- `tools/table_check.py` — 선수 판단에 대고. 2판, 1번 추천이 둘 다 맞다.
- `tools/learn_choices.py` — 프로가 무엇 대신 무엇을 골랐나. 1등 19% → 25%.
