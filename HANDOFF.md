# 진행 중인 일

세션은 끝나고 대화는 압축된다. **몇 시간짜리 일을 띄우면 그 즉시 여기 적는다** —
무엇이 돌고 있는지, 끝나면 무엇을 해야 하는지. 적지 않으면 다음 세션은 그것이
있는 줄도 모르고, 실제로 대기 고리 하나가 13시간을 멈춰 있는 동안 아무도
몰랐다.

끝난 일은 지운다. 이 파일은 기록이 아니라 **지금 떠 있는 것의 목록**이다.
지난 일은 git 로그와 `ref/`에 있다.

`bash tools/state.sh`가 이 파일의 아래 절을 함께 보여준다.

## 지금 돌고 있는 것 (2026-09-20 오후)

- **`node tools/enumerate_alternatives.js`** — 프로 배치 1,801개에 탐색을 돌려
  **버린 길**을 만드는 중. 배치당 8.7초라 약 **4시간**. 로그 `data/_alternatives.log`,
  결과 `data/alternatives.jsonl` (이어받기 됨).
  - 감시가 걸려 있다: 끝나면 재현율(유형별)과 `learn_choices.py` 교차검증이
    자동으로 돈다.
  - 이번 판에는 새 플레이 + 합쳐진 `route.js` + 새 꼬리표(리버스·세워치기·더블)가
    다 들어간다.

## 끝나면

1. 재현율을 유형별로 센다. 몰려 있으면 내 탐색의 구멍, 흩어져 있으면 그와 함께
   볼 자리. **맛세로 친 플레이는 원리상 못 찾는다** (`ref/terms.md`).
2. 교차검증 점수를 보고 **배운 식을 조언판 순위에 넣을지** 결정한다.

## 오늘 정리된 것

- 파이프라인 완주: 영상 41 · 스캔 39 · **플레이 1,816 / 조언판 1,585** (1,500 통과)
- 영상 39개 347 GB 지움. 주소는 `data/videos.json`에 (git 추적).
  362 GB → 13 GB.
- 앱 실행이 18.1초였던 것을 8.7초로. 걸어치기 재현 10/10 유지.

## 물어볼 것이 남은 것

- **횡단 vs 대회전**: 다섯 번 오간 것이 어느 쪽인가. *"잘 모르겠어"*.
  장-장-장-장-장 플레이에 이름을 붙여 주면 갈린다.
- **`POWC2026 201776661`** (7.3 GB) — 보정되는 프레임이 없어 스캔 불가. 주소는
  알고 있다. 지울지 둘지.
